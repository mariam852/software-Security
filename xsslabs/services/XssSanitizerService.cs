namespace xsslabs.services
{
    // Minimal placeholder sanitizer service for development
    public class XssSanitizerService
    {
        public string Sanitize(string input)
        {
            // Intentionally simplistic for lab purposes
            return input;
        }
    }
}
